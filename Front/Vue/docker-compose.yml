version: "3.9"

services:
  vue:
    image: node:20
    container_name: vue-app
    working_dir: /app
    volumes:
      - ./:/app  
    ports:
      - "8080:8080" 
    command: sh -c "npm install && npm run serve -- --host 0.0.0.0"
    user: "${UID}:${GID}"  
